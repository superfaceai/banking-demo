{
  "scripts": {
    "test": "dotenv -e .env.example -- jest",
    "test:record": "SUPERFACE_LIVE_API='*' dotenv -e .env -e .env.example -- jest -u"
  },
  "dependencies": {
    "@superfaceai/one-sdk": "^1.3.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@superfaceai/testing": "^2.0.3",
    "@types/jest": "^27.4.1",
    "dotenv-cli": "^5.1.0",
    "jest": "^27.5.1",
    "nock": "^13.2.4",
    "ts-jest": "^27.1.4"
  }
}
